name: Test Build

on:
  push:
    branches: [ master ]

jobs:
  setup-path:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    - run: echo $HOME/work/crafti/crafti/.github/workflows/ndless-sdk-bin/ >> $GITHUB_PATH
  install-dependancies:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    - run: |
          echo $HOME/work/crafti/crafti/.github/workflows/ndless-sdk-bin/ >> $GITHUB_PATH
          sudo apt install git gcc binutils libgmp-dev libmpfr-dev libmpc-dev zlib1g zlib1g-dev wget libboost-program-options-dev -y
          ls  $HOME/work/crafti/crafti/.github/workflows/ndless-sdk-bin/
          echo $PATH
